import { Component } from '@angular/core';

@Component({
  standalone: true,
  selector: 'cart-ticket',
  template: ` <p>hello</p> `,
})
export class CartTicketComponent {
  // component logic
  /*
  user$ = this.store.select('User');
  ngOnInit() {
    this.authService.user$.subscribe((user) => {
      this.userId = user.id;
    });
  }
  */
}
